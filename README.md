# Thermal-AI

Hybrid neural–numerical 2D heat diffusion simulator in the browser.

## Structure
- `solver-rust-wasm/`: Rust + WASM diffusion solver
- `web-ui/`: React UI demo
- `ml/`: dataset generation, training, ONNX export, evaluation
- `benchmarks/`: logs and plots

## Protocol
See `protocol.md`.
